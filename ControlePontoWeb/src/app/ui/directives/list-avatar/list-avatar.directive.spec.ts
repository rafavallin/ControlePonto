import { TCListAvatarDirective } from './list-avatar.directive';

describe('TCListAvatarDirective', () => {
  it('should create an instance', () => {
    const directive = new TCListAvatarDirective();
    expect(directive).toBeTruthy();
  });
});
