import { TCListIconDirective } from './list-icon.directive';

describe('TCListIconDirective', () => {
  it('should create an instance', () => {
    const directive = new TCListIconDirective();
    expect(directive).toBeTruthy();
  });
});
