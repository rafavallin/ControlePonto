import { TCListLineDirective } from './list-line.directive';

describe('TCListLineDirective', () => {
  it('should create an instance', () => {
    const directive = new TCListLineDirective();
    expect(directive).toBeTruthy();
  });
});
